import Ionicons from '@expo/vector-icons/Ionicons';
import { Pressable, StyleSheet, Text } from 'react-native';
import type { Coordinate } from './property-search.types';
export function MapSelectionSurface({ enabled, coordinate, target, onSelect }: { enabled: boolean; coordinate: Coordinate | null; target: { coordinate: Coordinate; delta: number } | null; onSelect: (value: Coordinate) => void }) { return <Pressable accessibilityRole="button" accessibilityLabel="Web harita alternatifi, konum noktası seç" disabled={!enabled} onPress={() => onSelect(target?.coordinate ?? { latitude: 39, longitude: 35 })} style={[s.surface, !enabled && s.disabled]}><Ionicons name={coordinate ? 'location' : 'map-outline'} size={38} color="#0369A1" /><Text style={s.title}>{enabled ? 'Web harita önizlemesi' : 'Önce mahalle seç'}</Text><Text style={s.copy}>{coordinate ? 'Nokta seçildi; bina kaydı doğrulanmadı.' : 'Etkileşimli harita iOS ve Android’de açılır. Web’de seçili bölgenin merkezi veya manuel giriş kullanılır.'}</Text></Pressable>; }
const s = StyleSheet.create({ surface: { minHeight: 220, alignItems: 'center', justifyContent: 'center', backgroundColor: '#E0F2FE', borderWidth: 1, borderColor: '#7DD3FC', borderRadius: 20, padding: 22, marginTop: 8 }, disabled: { opacity: 0.5 }, title: { color: '#0F172A', fontWeight: '800', marginTop: 10 }, copy: { color: '#475569', fontSize: 12, lineHeight: 18, textAlign: 'center', marginTop: 6 } });
